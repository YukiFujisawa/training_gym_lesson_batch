source 'https://rubygems.org'

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# brew install chromedriver
gem 'nokogiri'
gem 'page-object'
gem 'selenium-webdriver'

# Specify your gem's dependencies in training_gym_lesson_batch.gemspec
gemspec
