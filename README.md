# TrainingGymLessonBatch

## Installation

Add this line to your application's Gemfile:

```ruby
gem 'training_gym_lesson_batch'
```

And then execute:

    $ bundle

Or install it yourself as:

    $ gem install training_gym_lesson_batch